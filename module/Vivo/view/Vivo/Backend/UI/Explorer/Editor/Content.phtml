<?
$formType       = \DluTwBootstrap\Form\FormUtil::FORM_TYPE_HORIZONTAL;
foreach ($form->get('content')->getElements() as $name=>$element) {
    if($name != 'state') {
        echo $this->formRowTwb($element, $formType);
    }
}
echo $this->editorComponent;
?>
<hr />
<?php
echo $this->formRowTwb($form->get('content')->get('state'), $formType);
?>
