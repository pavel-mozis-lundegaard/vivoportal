<span>SITENAME ...</span>
<ul class="nav">
<?foreach($availableSites as $site):
$hosts = $site->getHosts();
?>
<?if (isset($selectedSite) && $selectedSite->getName() == $site->getName()):?>
<li class="active">
<?else:?>
<li>
<?endif;?>
<a href="<?=$this->url(null, array ('host' => current($hosts)))?>"><?=$site->getName()?></a>
</li>
<?endforeach;?>
</ul>
