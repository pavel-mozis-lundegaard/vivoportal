<?
    $formType       = \DluTwBootstrap\Form\FormUtil::FORM_TYPE_HORIZONTAL;
    $this->tinyMce('exact', $form->get('resource')->getAttribute('id'), true, 'en');
    echo $this->formFieldsetTwb($form, $formType);
?>