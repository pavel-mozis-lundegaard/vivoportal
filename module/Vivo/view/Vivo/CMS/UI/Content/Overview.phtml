<? if(count($children)) : ?>
	<ul class="overviewDefault">
	<? foreach($children as $child) : ?>
		<li><a href="<?=$this->document($child)?>"><?=$child->getTitle();?></a></li>
	<? endforeach; ?>
	</ul>
<? endif; ?>