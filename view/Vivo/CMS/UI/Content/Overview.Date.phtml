<div class="OverviewDate">
<? if(count($children)) : ?>
	<ul>
	<? foreach($children as $child) : ?>
		<li>
			<?=$child->getPublished()->format('d.m.Y') ?> <a href="<?=$this->document($child)?>"><?=$child->getTitle();?></a>
        </li>
		<? endforeach; ?>
	</ul>
<? endif; ?>
</div>