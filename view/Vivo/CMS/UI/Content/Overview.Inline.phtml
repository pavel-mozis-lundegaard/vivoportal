<div class="OverviewInline">
<? if(count($children)) : ?>
	<ul class="inline">
	<? foreach($children as $child) : ?>
		<li><a href="<?=$this->document($child)?>"><?=$child->getOverviewTitle();?></a></li>
	<? endforeach; ?>
	</ul>
<? endif; ?>
</div>